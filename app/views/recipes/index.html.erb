<% @receipts.each do |recipe| %>
  <%= image_tag recipe.image %>
  <%= recipe.title %>
  <%= recipe.price %>
  <%= recipe.time %> min
  <%= recipe.difficulty %>
  <%= link_to "Voir la recette", recipe %>
  <%= l recipe.created_at, format: "%d/%m/%Y" %>
<% end %>
